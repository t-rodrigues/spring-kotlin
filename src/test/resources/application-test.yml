spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/mercado_livro_tests}
    username: ${DATABASE_USER:postgres}
    password: ${DATABASE_PASSWORD:docker}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true
